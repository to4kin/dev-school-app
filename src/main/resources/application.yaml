server:
  port: 8080

spring:
  application:
    name: DevSchool-Application-2
  datasource:
    url: jdbc:postgresql://localhost:5432/dev-school
    driverClassName: org.postgresql.Driver
    username: postgres
    password: 4444
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: false


base:
  path: http://localhost:8080
